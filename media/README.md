# media/

Drop the source files for Phase 1 here:

- The screen-recorded ListingLeads webinar (`.mp4` / `.mov` / `.mkv` / `.webm`)
- The screenshot of the tool's recipient-list screen (`.png` / `.jpg`)

Nothing in this folder is committed to git — the recording is large and is
source material, not project code.
